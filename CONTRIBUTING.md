# Contributing

1. Create a branch from `main`.
2. Keep changes focused and include tests for changed behavior.
3. Run `npm run build` in `frontend`.
4. Run `./mvnw clean verify` at the repository root.
5. Open a pull request that explains the user impact and validation performed.

Never commit `.env` files, database dumps, credentials, or access tokens.
